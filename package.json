{
  "devDependencies": {
    "electron": "^15.3.0"
  },
  "dependencies": {
    "@types/react-redux": "^7.1.20",
    "esbuild": "^0.13.12",
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "react-redux": "^7.2.6"
  },
  "scripts": {
    "build": "esbuild ./src/renderer/app.tsx --bundle --outfile=./src/pano.js",
    "start": "electron ./src",
    "wasm": "cd pano-rs && wasm-pack build --target web && cargo fmt"
  }
}
