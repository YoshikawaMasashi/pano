{
  "devDependencies": {
    "electron": "^15.3.0"
  },
  "dependencies": {
    "esbuild": "^0.13.12",
    "react": "^17.0.2",
    "react-dom": "^17.0.2"
  },
  "scripts": {
    "build": "esbuild ./src/renderer/app.jsx --bundle --outfile=./src/pano.js",
    "start": "electron ./src",
    "wasm": "cd pano-rs && wasm-pack build --target web"
  }
}
